services:
  postgres:
    image: postgres:16-alpine
    container_name: crossvpn-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: crossvpn
      POSTGRES_USER: crossvpn
      POSTGRES_PASSWORD: crossvpn
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crossvpn -d crossvpn"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crossvpn-app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
    command: sh -c "npx prisma db push --skip-generate && node dist/main.js"

volumes:
  postgres_data:
